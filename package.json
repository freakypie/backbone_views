{
  "name": "backbone_views",
  "version": "0.1.4",
  "description": "A collection of views similar to django views",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "coffee --output lib --compile src",
    "pretest": "coffee --output lib --compile src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "dependencies": {
    "backbone": "~1.2.0",
    "forms": "^1.1.1",
    "underscore": "~1.8.3"
  },
  "keywords": [
    "backbone",
    "views",
    "cbv"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/freakypie/backbone_views.git"
  }
}
